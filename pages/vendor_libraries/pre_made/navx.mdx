## NavX Lib

Out of date, but the first rust competition did create bindings for the navx library.

```toml copy
[dependancies]
arfur-rev = { git = "https://github.com/first-rust-competition/navx-rs", version = "0.1.0" }
```
