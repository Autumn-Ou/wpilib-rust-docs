## CTRE Lib

Out of date, but the first rust competition did create bindings for the ctre v5 api.

```toml copy
[dependancies]
navx-rs = { git = "https://github.com/first-rust-competition/ctre-rs", version = "0.6.1" }
```
